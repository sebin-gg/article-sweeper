version: 2
updates:
  # CI deps (requirements-ci.txt, hash-locked; skills-ref is pinned in
  # the same file). Bump pins via pip-compile on requirements-ci.in.
  - package-ecosystem: "pip"
    directory: "/"
    schedule:
      interval: "weekly"
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
