version: 2
updates:
  # github-actions only. Python CI deps are hash-locked in
  # requirements-ci.txt (bump via pip-compile on requirements-ci.in);
  # a pip ecosystem entry here would only duplicate that work.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
